{
  "name": "@wtf/pi-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "biome check . && pnpm run typecheck && vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/node": "^20.19.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
